export function secret(): string {
  // ToDo: call useState() to store turns
  // use conditional rendering to return two strings

  return "Win a round of minesweeper to gain my trust. When done call the secret command again.";

  // : "nice try but you haven't won yet."
}
